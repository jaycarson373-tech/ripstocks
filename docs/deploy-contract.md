# Deploying Pack #01

The Pons CA is **not required**. Follow the current [pre-CA setup and deployment procedure](start-here.md). The treasury key belongs only in Railway. Deployment uses the immutable price and universe in `config/packs.json`; the new contract starts disabled.
