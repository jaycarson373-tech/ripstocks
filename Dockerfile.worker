FROM node:22-bookworm-slim

WORKDIR /app
ENV NODE_ENV=production

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY worker ./worker
COPY config ./config

CMD ["npm", "run", "treasury-worker"]
